CREATE TABLE stats
(id INTEGER PRIMARY KEY,
location VARCHAR(100) NOT NULL,
gold BIGINT NOT NULL DEFAULT 20000000 CHECK (gold >= 0),
manpower INTEGER NOT NULL DEFAULT 0,
default_defense TEXT NOT NULL DEFAULT 'soldiers,tanks,artillery'
);
